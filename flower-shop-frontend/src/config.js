// API base of your backend (Telegram bot's web API if exposed) or a mock JSON server.
// For local demos you can keep it empty and use a mock.
export const API_BASE = ""; // e.g. "http://localhost:8000" or "" to use mock data
